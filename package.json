{
  "name": "mannysgame",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ shahruz/mannys-game",
    "create-local": "graph create --node http://localhost:8020/ shahruz/mannys-game",
    "remove-local": "graph remove --node http://localhost:8020/ shahruz/mannys-game",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 shahruz/mannys-game"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
